streamlit==1.26.0
web3==6.9.0
eth-account==0.9.0
requests==2.31.0
pandas==2.1.0

Optional (if email functionality is used):
secure-smtplib
Note: You can run pip install -r requirements.txt to reproduce the environment.
